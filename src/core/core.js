define(function(require, exports, module) {
    window.api = {};
    window.extensions = {};
    var $$util = window.api.util = require('./util/util');
    var $$router = window.api.router = require('./router/router');







    window.tmpFn.forEach(function (item) {
        item();
    });
});