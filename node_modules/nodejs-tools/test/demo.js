var tool = require('../src/main.js');

var fs = require('fs');
var htmlString = fs.readFileSync('./test/demo.html', 'utf8');
var re = tool.htmlToJs(htmlString);
var re = tool.htmlToJsNs(htmlString, 'app.demo');
console.log(re);
